{
  "name": "@forge/mcp",
  "version": "0.1.0",
  "description": "FORGE MCP server — tool-only, no internal LLM calls",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "forge-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "@forge/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  }
}
